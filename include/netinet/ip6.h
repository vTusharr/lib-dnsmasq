/* Intentionally empty: shim so dnsmasq's #include resolves as we dont have this headerr . The features that would use its contents are
 * compiled out (NO_DHCP/NO_TFTP) or their symbols come from lwIP/musl. */
